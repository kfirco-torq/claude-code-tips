Only use when blocked on a decision the user must make. Users can always select "Other". Use multiSelect: true for multiple answers. In plan mode, use ${vd} to enter plan mode; use this tool only to clarify requirements. Do NOT ask "Is my plan ready?" — user can't see plan until ${FW}.
